#include <stdio.h>

int main(){

    /*
    Incremento (++)
    Pré-Incremento ++a
    Pós-Incremento a++
    Descremento (--)
    Pré-Descremento --a
    Pós-Descremento a--
    */
   int numero1 = 1;
   printf("Antes Incremento: %d\n", numero1);
   //numero1 = numero1 +1
   //numero1 += 1;
   numero1++;
   printf("Pós Incremento: %d\n", numero1);

   numero1--;
   printf("Pós Descremento: %d\n", numero1);








}