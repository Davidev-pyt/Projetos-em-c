#include <stdio.h>
//Entrada de Dados

int main(){
    int idade = 18;
    float altura = 1.83;
    char opcao = 'S';
    char nome[25] = " Davi ";

   // printf("A idade do %s é: %d\n", nome , idade);
    printf("A idade é: %d\n", idade);
    printf("O nome é: %s\n", nome);
    printf("A altura é: %.2f\n", altura);
    printf("A opção é: %c\n", opcao);


/*
    %d: imprime um inteiro no formato decimal 
    %i: equivalente ao %d
    %f: imprime um numero de ponto flutuante de formato  padrão
    %e; imprime um numero de ponto flutuante em notação cientifica
    %c: imprime um unico caractere
    %s: imprime uma cadeia (string) de caractere



*/
}

